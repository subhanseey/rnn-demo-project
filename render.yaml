services:
  - type: web
    name: rnn-demo-project
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run main.py --server.port 10000
    plan: free
    runtime: python-3.10.13
    